# SSSF uses the Codex CLI's saved ChatGPT subscription authentication.
# Run `codex login`; do not place API keys in this file.

# Optional overrides
# CODEX_PATH=codex                  # Codex binary if it is not on PATH
# ENGINEER_NAME=                    # lane label; defaults to git user.name
